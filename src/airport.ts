type Passenger = {
    firstName: string,
    lastName: string,
    gender?: string,
    race?: string,
    covid: boolean,
    ticket: number,
}

const issueTicket = () => {
    return
}

const boardPlane = () => {
    return
}